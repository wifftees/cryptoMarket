export type RegisterFormValues = {
    firstName: string
    secondName: string
    email: string
    password: string
    confirm: string
}

export type LoginFormValues = {
    email: string
    password: string
}

export type User = {
    firstName: string
    secondName: string
    email: string
    password: string
    avatar: string
    description: string
}
